## Required

# OpenRouter API key — how Kady reaches hosted models (OpenAI, Anthropic,
# Google, xAI, Qwen, …). Only required if you use OpenRouter models; if you
# run everything locally through Ollama, this can be left blank.
OPENROUTER_API_KEY=

# Optional: override the OpenRouter base URL. Because the provider is just an
# OpenAI-compatible endpoint, you can point it at any compatible gateway and
# keep using OPENROUTER_API_KEY for that gateway's key. For example, Requesty
# (https://requesty.ai) — caching, failover, and cost controls, same
# "vendor/model" ids:
#   OPENROUTER_BASE_URL=https://router.requesty.ai/v1
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1


## Model defaults (optional)

# Default model for the agent. This is a bare provider model id; the provider
# is set by DEFAULT_MODEL_PROVIDER below. You can also switch models per chat
# tab in the UI. (There is a single agent now — no separate "expert" model.)
DEFAULT_MODEL_ID="anthropic/claude-opus-4.8"
DEFAULT_MODEL_PROVIDER="openrouter"


## Optional: local Ollama server

# Host for a running `ollama serve`. Models pulled into Ollama show up in the
# model dropdown (as `ollama/<name>`) and run directly against your local
# daemon. Pick `DEFAULT_MODEL_ID` like `llama3` with DEFAULT_MODEL_PROVIDER or
# just choose an `ollama/*` model in the UI.
OLLAMA_BASE_URL=http://localhost:11434


## Optional: web access (pi-web-access)

# Web search, URL/PDF fetching, GitHub repo reading, and video understanding
# work out of the box with no key (Exa MCP fallback). Add keys to unlock the
# direct providers — they can also be set live from Settings → API keys.
# EXA_API_KEY=            # exa.ai — fast web + code search
# PERPLEXITY_API_KEY=     # perplexity.ai — synthesized answers with citations
# GEMINI_API_KEY=         # Google AI Studio — search fallback + YouTube/video understanding


## Optional: backend knobs

# Override the backend port / projects directory if needed.
# KADY_PORT=8000
# KADY_PROJECTS_ROOT=./projects
# Bind address for the backend. Defaults to loopback-only; set to 0.0.0.0
# to accept connections from other hosts (e.g. a reverse proxy on another
# machine/container).
# KADY_HOST=127.0.0.1
# Python interpreter used only for the .h5ad preview helper.
# KADY_PYTHON=python3


## Optional: reverse proxy / sub-path deployment

# Set these when Kady is served behind a reverse proxy that forwards the
# full request path unstripped (e.g. a JupyterHub-style service proxy),
# rather than directly on http://localhost:8000 / :3000.
#
# Path the backend's routes are mounted under. Must start with "/". Example
# for a proxy that forwards https://host/kady/byok/* straight through:
# BACKEND_URL_PREFIX=/kady/byok
#
# Path the frontend (Next.js) is mounted under. Sets Next's basePath /
# assetPrefix so pages and static assets resolve correctly.
# FRONTEND_URL_PREFIX=/kady/app
#
# Base URL the browser uses to reach the backend. Point it at
# BACKEND_URL_PREFIX above (a bare path works — it resolves relative to the
# page's own origin, so no scheme/host is needed when frontend and backend
# share a domain through the proxy).
# NEXT_PUBLIC_ADK_API_URL=/kady/byok
#
# Extra origins (exact match, comma-separated) allowed to call the backend,
# in addition to the built-in localhost/LAN allow-list. Needed when the
# public hostname isn't localhost/a private LAN IP.
# CORS_ALLOW_ORIGINS=https://your-public-host.example.com


## Not used yet — being re-added as native tools

# The integrations below were part of the previous architecture and are not
# read by the current backend. They're listed here so you don't lose your
# values; literature search, document conversion, and remote compute will
# return as native tools in upcoming releases. (Web search is back — see the
# "web access" section above.)

# Web search
# PARALLEL_API_KEY=

# Paperclip literature / regulatory / clinical-trial search
# PAPERCLIP_API_KEY=

# Modal remote compute
# MODAL_TOKEN_ID=
# MODAL_TOKEN_SECRET=

# Scientific databases and search
# NCBI_API_KEY=
# NCBI_EMAIL=
# S2_API_KEY=
# CORE_API_KEY=
# OPENALEX_API_KEY=

# Specialized scientific APIs
# ADAPTYV_API_KEY=
# ROWAN_API_KEY=
# MP_API_KEY=
# OMIM_API_KEY=
# BIOGRID_API_KEY=
# DISGENET_API_KEY=
# ADDGENE_API_KEY=
# CLUE_API_KEY=
# OPENFDA_API_KEY=
# PATENTSVIEW_API_KEY=
# DATACOMMONS_API_KEY=

# Government / economic data
# FRED_API_KEY=
# BEA_API_KEY=
# BLS_API_KEY=
# CENSUS_API_KEY=
# NASA_API_KEY=
# NOAA_API_KEY=
# OPENWEATHERMAP_API_KEY=
# ALPHAVANTAGE_API_KEY=

# Zotero reference manager
# ZOTERO_LIBRARY_ID=
# ZOTERO_API_KEY=
# ZOTERO_LIBRARY_TYPE=

# ML / AI platforms
# HUGGINGFACE_TOKEN=
# QISKIT_IBM_TOKEN=

# Other
# TILEDB_REST_TOKEN=
# GOOGLE_API_KEY=
# GOOGLE_CSE_ID=
